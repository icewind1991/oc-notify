oc-notify
====

oc-notify is a daemon written in node.js that watches the data directory of an ownCloud installation and triggers the ownCloud instance to update the filecache when changes are detected.

Installation
---
`npm install`

Usage
---

`node app.js /path/to/owncloud`
